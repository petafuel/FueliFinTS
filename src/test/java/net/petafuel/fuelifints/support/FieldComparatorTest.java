package net.petafuel.fuelifints.support;

import org.junit.Test;

public class FieldComparatorTest {

    @Test
    public void testCompare() throws Exception {
        /*
        Field[] fields = HNVSK.class.getDeclaredFields();
        List<Field> usedFields = new ArrayList<Field>();
        for(Field f: fields) {
            if(AbstractElement.getMatchingElementDescription(f,0) != null) {
                usedFields.add(f);
            }
        }
        Collections.sort(usedFields, new FieldComparator(0));

        for(int i = 0; i < usedFields.size(); i++) {
            if(usedFields.get(i).getAnnotation(ElementDescription.class).number()-1 != i) {
                fail();
            }
        }
        */
    }
}
