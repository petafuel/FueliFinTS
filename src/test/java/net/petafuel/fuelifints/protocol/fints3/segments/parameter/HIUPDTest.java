package net.petafuel.fuelifints.protocol.fints3.segments.parameter;

import org.junit.Test;

public class HIUPDTest {

    @Test
    public void testHIUPD() {
        /*
        DummyAccessFacade daf = new DummyAccessFacade();
        LegitimationInfo legitimationInfo = new LegitimationInfo();
        legitimationInfo.setUserId("100");
        ArrayList<AccountDataObject> dataObjects = daf.getAccountData(legitimationInfo);

        for (AccountDataObject data : dataObjects) {
            HIUPD hiupd = new HIUPD(data, 3);
        }
        */
    }
}
