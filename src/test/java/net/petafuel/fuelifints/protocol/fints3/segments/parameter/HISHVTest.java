package net.petafuel.fuelifints.protocol.fints3.segments.parameter;

import org.junit.Test;

public class HISHVTest {
    @Test
    public void testHISHV() {
        /*
        HISHV hishv = new HISHV(3);
        String hishvString = new String(hishv.getHbciEncoded());
        for (SecurityMethod method : SecurityMethod.values())
            assertTrue(hishvString.contains(new String(method.getHbciDEG())));
            */
    }
}
