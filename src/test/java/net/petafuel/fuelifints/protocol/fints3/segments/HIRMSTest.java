package net.petafuel.fuelifints.protocol.fints3.segments;

import org.junit.Test;

public class HIRMSTest {


    @Test
    public void testGetHbciEncoded() throws Exception {
        /*
        HIRMS hirms = new HIRMS(new byte[0]);
        Segmentkopf segmentkopf;// = new Segmentkopf("HIRMS:0:2:3".getBytes("ISO-8859-1"));
        segmentkopf = Segmentkopf.Builder.newInstance().setSegmentKennung(HIRMS.class).setSegmentVersion(2).setBezugssegment(3).build();
        hirms.setSegmentkopf(segmentkopf);
        List<Rueckmeldung> rueckmeldungen = new LinkedList<Rueckmeldung>();
        Rueckmeldung rueckmeldung = new Rueckmeldung("3010::Auftrag Teilweise ausgeführt.".getBytes("ISO-8859-1"));
        rueckmeldungen.add(rueckmeldung);
        rueckmeldung = new Rueckmeldung("0010::Auftrag ausgeführt.".getBytes("ISO-8859-1"));
        rueckmeldungen.add(rueckmeldung);
        rueckmeldung = new Rueckmeldung("9910::Auftrag inhaltlich ungültig.".getBytes("ISO-8859-1"));
        rueckmeldungen.add(rueckmeldung);
        hirms.setRueckmeldung(rueckmeldungen);
        byte[] hbciEncoded = hirms.getHbciEncoded();
        LOG.info(new String(hbciEncoded, "ISO-8859-1"));
        */
    }
}
